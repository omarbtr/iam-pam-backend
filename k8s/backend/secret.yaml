apiVersion: v1
kind: Secret
metadata:
  name: backend-secret
  namespace: iam-pam
type: Opaque
stringData:
  DB_PASSWORD: postgres
  KEYCLOAK_CLIENT_SECRET: "change-me"
  RESOURCE_ENCRYPTION_KEY: "change-me-32-char-key-here!!!!!"
  FACE_JWT_SECRET: "change-me-face-jwt-secret"
  # Base64-encoded private key for bastion SSH access.
  # Generate: base64 -w 0 /path/to/bastion_key
  BASTION_KEY: ""
